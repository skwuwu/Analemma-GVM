[package]
name = "gvm-engine"
version = "0.4.0"
edition = "2021"
description = "GVM Governance Engine — policy evaluation module (Wasm-compatible)"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
crate-type = ["cdylib", "rlib"]
